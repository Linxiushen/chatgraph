name: ChatGraph
on:
  push:
    paths: ['chatgraph/**', '.dockerignore', '.github/workflows/chatgraph.yml']
  pull_request:
    paths: ['chatgraph/**', '.dockerignore', '.github/workflows/chatgraph.yml']
  workflow_dispatch:
permissions:
  contents: read
jobs:
  node-compatibility:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node --test chatgraph/test/*.test.mjs
  container-recovery:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: node chatgraph/scripts/deploy-check.mjs
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: node --test chatgraph/test/*.test.mjs
      - run: node --test chatgraph/extension/test/*.test.mjs
      - run: node chatgraph/mobile/build-shortcut.mjs --check
      - run: node scripts/generate-viewer.mjs --check
      - run: npm --prefix chatgraph/integrations/chatgpt ci --ignore-scripts
      - run: npm --prefix chatgraph/integrations/chatgpt test
      - run: npm --prefix chatgraph/integrations/chatgpt audit --omit=dev --audit-level=high
      - run: npm install --no-save --package-lock=false --prefix chatgraph playwright@1.62.1
      - run: node chatgraph/node_modules/playwright/cli.js install --with-deps chromium
      - run: npm run test:browser --prefix chatgraph
        env:
          CHATGRAPH_PLAYWRIGHT_PATH: ${{ github.workspace }}/chatgraph/node_modules/playwright/index.mjs
      - run: node chatgraph/integrations/browser-check.mjs
        env:
          CHATGRAPH_PLAYWRIGHT_PATH: ${{ github.workspace }}/chatgraph/node_modules/playwright/index.mjs
      - run: node chatgraph/mobile/browser-check.mjs
        env:
          CHATGRAPH_PLAYWRIGHT_PATH: ${{ github.workspace }}/chatgraph/node_modules/playwright/index.mjs
      - run: node chatgraph/scripts/package-release.mjs
      - uses: actions/upload-artifact@v4
        with:
          name: chatgraph-beta
          path: chatgraph/test-output/releases/
